const numeros = [0, 3, 18, 2, 11];

let i = 0;

while (i < numeros.length) {//enquanto o i for menor que o array
    //executar isso
    console.log(numeros[i]);
    i++; //fazendo o incremento a cada item faz o loop varrer todos os itens do array

}