const paises = ["Brasil", "Argentina", "México", "Uruguai", "Chile"];

for (let i = o; i < 5; i++) { //faz exaramente o while no exemplo anterior, uma forma mais enxuta
    console.log(paises[i]);
}
