
const paises = ["Brasil", "Argentina", "México", "Uruguai", "Chile"];

let indice = 0;

while (indice < paises.length) { //aqui ele diz que enquanto indice for menor que a quantidade de itens dentro de países.
    // length faz essa contagem de itens dentro do array.  
    console.log(paises[indice]);
    indice++; //toda vz que entrar dentro de while ele vai acrecentar +1 a indice.
}